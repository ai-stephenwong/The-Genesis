# Module: {module-name}

> Generated by solution-architect during module decomposition.
> This file scopes exactly what one developer sub-agent builds.

## Scope

**Owns:** {list of directories/files this module owns}
**Does NOT own:** {explicitly excluded areas — prevents overlap}

## Interface Contract

### Exports (what this module provides to others)

| Export | Type | Signature / Shape | Consumed by |
|---|---|---|---|
| {function/component/API} | {function / React component / REST endpoint / event} | {TypeScript signature or endpoint path} | {module name(s)} |

### Imports (what this module consumes from others)

| Import | Type | Provided by | Notes |
|---|---|---|---|
| {function/component/API} | {function / React component / REST endpoint / event} | {module name} | {any constraints} |

## Data Structures

Shared types used by this module (must match `api-spec.yaml` and other modules' contracts):

```typescript
// Paste or reference the relevant types from the shared types module
```

## Dependencies

| Dependency | Module | Required before build? | Notes |
|---|---|---|---|
| {what is needed} | {which module provides it} | {Yes/No} | {details} |

## Build Order

**Phase:** {0 = shared/common, 1 = independent, 2 = integration-dependent}
**Can run in parallel with:** {list of modules in the same phase with no mutual dependency}
**Must wait for:** {list of modules that must complete first}

## Requirements Traceability

| Requirement ID | Section in functional-specs.md | Covered by this module? |
|---|---|---|
| {REQ-xxx} | {section ref} | {Yes / Partial / No — if partial, which module covers the rest} |

## Acceptance Criteria

- [ ] All exports listed above are implemented and usable
- [ ] All imports are consumed correctly (no type mismatches)
- [ ] Unit tests cover all exported functions/components
- [ ] Build passes in isolation (`npm run build` / equivalent for this module's scope)
- [ ] No references to files outside this module's owned directories (except imports listed above)
